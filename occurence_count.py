#take a string and another substr from user as input and find occurence of that substring

string = input("Enter Your String : ")
substr = input("Enter your Substring :")

print("Occurence of your substring is : ",string.count(substr))
